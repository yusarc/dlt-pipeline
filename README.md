# dlt-pipeline

## dlt API → DuckDB Pipeline

Minimal example of an end-to-end data pipeline using `dlt` and an AI-assisted IDE.

## What this project shows

- Load data from a public HTTP API using `dlt`
- Normalize and store it as relational tables in DuckDB
- Inspect pipeline state, schema, and data locally

## Requirements

- Python 3.11+
- `uv` or `pip` for dependencies
